env = ${env}
service.host = ${service.host}
service.type = ${service.type}
template.body.path = ${template.body.path}
template.header.path = ${template.header.path}
template.data.path = ${template.data.path}